This is an example of a real-life typescript project.

Setup
```console
foo@bar:~$ npm init -y # start project
foo@bar:~$ npm i -D typescript # install typescript
foo@bar:~$ tsc --init # create a config
```
